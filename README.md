This is an application to view, sort and filter data.

# Set up

npm install

# Steps to run the project:

npm start

Once successful, the browser should open the application at http://localhost:3000/